const { COMMON_KEYS } = require("./commonModel");
const { USER_KEYS } = require("./user");
const { TOKEN_KEYS } = require("./accessToken");
const { PATIENT_KEYS } = require("./patient");
const { DOCTOR_KEYS } = require("./doctor");
const { APPOINTMENT_KEYS } = require("./appointment");

module.exports = {
    COMMON_KEYS,
    USER_KEYS,
    TOKEN_KEYS,
    PATIENT_KEYS,
    DOCTOR_KEYS,
    APPOINTMENT_KEYS
};